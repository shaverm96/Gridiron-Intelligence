---
title: "Gridiron Intelligence: Multi-Agent Football Scouting Application"
author:
  - name: "Matthew Shaver"
    affiliation: "UNC Charlotte"
    email: "mshaver5@charlotte.edu"
  - name: "Elliott Kervin"
    affiliation: "UNC Charlotte"
    email: "ekervin@charlotte.edu"
format:
  html:
    toc: true
    number-sections: true
  pdf:
    toc: true
    number-sections: true
    documentclass: report
    classoption:
      - 11pt
      - oneside
    geometry:
      - margin=1in
---

## 2. System Architecture & Methodology

Gridiron Intelligence is built as a modular, multi-layered system that keeps the user interface separate from orchestration, data access, and agent reasoning. This structure supports both deterministic report generation and more flexible multi-step analysis.

### 2.1 Modular Layered Design
The application is organized into four broad layers:

* **UI layer:** Handles page navigation, user input, and report display.

* **Orchestration layer:** Chooses the workflow for recruiting, transfer, or chat requests.

* **Data and tool layer:** Pulls structured records, API data, and web context.

* **Agent layer:** Coordinates planning, evidence gathering, and synthesis.


### 2.2 Workflow Orchestration
The system uses two complementary orchestration patterns.

#### 2.2.1 Recruiting Portal (Sequential Enrichment)
The recruiting workflow starts with a structured player record and then enriches it with vector insights, web context, and historical comparables before final synthesis. The goal is to keep the report grounded in stable data while still adding useful current context.

```{mermaid}
flowchart TD
    A[User Inputs: Year Player Team] --> B[Rate Limit Guard]
    B --> C[Fetch Player Bundle from Supabase]
    C --> D[Vector Insights Lookup]
    C --> E[Structured Web Graph]
    C --> F[Historical Comparables]
    C --> G[Score Card Build]

    E --> E1[Recruiting Scout Node]
    E1 --> E2[Team Scout Node]

    D --> H[Recruiting Final Prompt Assembly]
    E2 --> H
    F --> H
    G --> H
    C --> H

    H --> I[Gemini Final Synthesis]
    I --> J[Recruiting Report Output]
    J --> K[Recruiting Open Chat]
```

#### 2.2.2 Transfer Portal (Parallel Multi-Source Extraction)
The transfer workflow also begins with structured identity resolution, then gathers supporting evidence from multiple sources in parallel. This keeps the process fast while preserving a clear chain from identity to analysis.

```{mermaid}
flowchart TD
    A[User Inputs: Position Candidate Team] --> B[Rate Limit Guard]
    B --> C[Fetch College Player Bundle]

    C --> D[Parallel Branches]
    D --> D1[CFBD Context Branch]
    D --> D2[Player News Search Branch]
    D --> D3[Team News Search Branch]

    D1 --> E1[orchestrate_transfer_cfbd_context]
    E1 --> E2[Usage and Season Stats Pulls]
    E2 --> E3[Compact Tables and Diagnostics]

    D2 --> F1[search_web_query_tool]
    F1 --> F2[summarize_payload_tool]

    D3 --> G1[search_web_query_tool]
    G1 --> G2[summarize_payload_tool]

    E3 --> H[Transfer Synthesis Prompt]
    F2 --> H
    G2 --> H
    C --> H

    H --> I[Gemini Final Transfer Synthesis]
    I --> J[Transfer Output]
    J --> K[Side by Side Charts]
    K --> L[Tables]
    L --> M[Transfer Debug Details]
    M --> N[Transfer Open Chat]
```

#### 2.2.3 Open Chat (Fan-out/Fan-in Topology)
Open chat uses the same general orchestration pattern, but it is optimized for follow-up questions and context-aware responses. A delegator routes the request, specialized workers gather evidence, and the synthesizer combines the results into the final answer.

```{mermaid}
flowchart LR
  Q[User Query] --> D[Lead Delegator]
  D --> C[Structured Data Branch]
  D --> W[Web Context Branch]
  C --> S[Lead Synthesizer]
  W --> S
  S --> R[Final Response]
```

### 2.3 State and Memory Contract
The system maintains session stability through a dedicated state model. To avoid context growth over time, it uses bounded memory through truncation rather than summarization.

* **State compaction:** Only the most recent turns, trace entries, errors, and citations are kept.

* **Bulky context removal:** Large intermediate data is cleared after it is no longer needed for the current turn.

* **Session scope:** Open chat is seeded from the current report context when available, but it does not carry memory across separate sessions.

## 3. Data Strategy & Management

The effectiveness of scouting evaluations depends on the precision of player identification and the ability to merge static historical records with live performance metrics. Our data strategy prioritizes a "SQL-first" approach to ensure reliability and traceability.

### 3.1 Multi-Source Data Integration
The system aggregates data from four distinct channels:
* **Supabase (Primary Data Lake):** Stores canonical player bundles, historical physical data, and model prediction thresholds.
* **College Football Data (CFBD) API:** Provides real-time college-level statistics, including seasonal totals and per-game usage metrics.
* **DuckDuckGo Web Search:** Supplies unstructured, real-time context such as injury updates, transfer rumors, and team depth chart news.
* **Vector Datastore:** Contains embedded qualitative "factoids" retrieved via similarity search to enrich prospect profiles.

### 3.2 Split-Identity Data Model
A core technical contribution of this project is the refactoring of the database from a single player table to a **split-identity model**. This architecture explicitly separates a player’s high school recruiting phase from their college career to handle the complexities of the transfer portal.

* **`gi_recruit_master`**: The source of truth for high school profiles, star ratings, and commitment history.
* **`gi_college_master`**: The source of truth for college-level identity, mapped to CFBD athlete IDs.
* **`gi_player_link_bridge`**: A crosswalk table that links recruit identities to college records, enabling the system to trace a prospect's development from high school into the transfer portal.

### 3.3 Identity Resolution Policy
To avoid the "Player Identity Crisis"—where an LLM might confuse two players with the same name—Gridiron Intelligence implements a strict **SQL-first fuzzy matching** policy. 

Rather than relying on probabilistic vector embeddings for identity lookups, the system uses the PostgreSQL `pg_trgm` (trigram) extension. This allows for high-precision text matching across name, position, and team fields. If the system identifies multiple potential matches (e.g., two players named "Josh Williams"), it is programmed to pause and trigger a clarification flow for the user rather than proceeding with potentially incorrect data.

### 3.4 Data Characteristics and Quality
The system is built upon a master dataset covering prospects from 2015 to 2028. For performance and relevance, the application defaults to a "Top 100" window by rating for current recruiting classes.

| Dataset/Table | Scope | Primary Use |
| :--- | :--- | :--- |
| **Master Recruits** | ~2015–2028 | Historical comparables and prospect lookups. |
| **CFBD Athletes** | Live API | Real-time usage and performance bars. |
| **Model Scores** | Projected | Probability-based success forecasting. |
| **Factoid Vectors** | Qualitative | Semantic enrichment of scouting reports. |

To ensure production stability, the data access layer implements **null-safe sorting** (prioritizing rating desc) and **diagnostics blocks** that check for missing critical fields (like position or team) before a report is generated.
